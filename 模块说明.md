# STM32F405RGT6 无人船控制固件 — 模块说明

本文档按目录说明工程内各文件/模块职责，便于后续维护。  
**维护时优先改 `User/` 与 `Core/` 中带 `USER CODE` 的区域；`Drivers/` 为 ST 官方库，一般不要手改。**

- 芯片：STM32F405RGT6（Cortex-M4，Flash 1MB，SRAM 192KB）
- 系统时钟：HSE 8MHz → PLL → SYSCLK **168MHz**（AHB 168MHz，APB1 42MHz，APB2 84MHz）
- 工具链：Keil MDK-ARM（工程文件在 `MDK-ARM/`）
- 业务定位：无人船主控。接收上位机 JSON 指令，采集 GNSS / 航向 / BMS，做导航控制，再把速度指令发给驱动板。

---

## 1. 系统数据流

```
上位机 PC
    │ JSON (UART4, 115200)
    ▼
protocol.c/h          解析指令 / 打包遥测
    │
    ▼
main.c                主循环调度、模式切换、保护逻辑
    │
    ├── gnss_sensor       USART2 / 4800 / Modbus 从站 0x50
    ├── heading_sensor    USART3 / 4800 / Modbus 从站 0x60
    ├── bms_sensor        USART1 / 9600 / Modbus 从站 0x01  （当前主循环未真正轮询）
    ├── navigation_controller   串级增量式 PI 导航
    └── UART6 9600        9 字节帧发给驱动板（0x55 ... 0xAA）
```

主循环顺序（`main.c`）：

1. 喂狗 `HAL_IWDG_Refresh`
2. `System_UpdateSensors`（50ms 交替更新 GNSS / 航向）
3. `System_UpdateTelemetry`（1s 上报 JSON）
4. `System_ProcessCommand`（处理上位机指令）
5. `System_CheckAutoReturn`（断连 / 低电量保护）
6. `System_UpdateNavigation`（100ms 导航计算）
7. `System_SendDriverCommand`（100ms 发驱动板）

---

## 2. 目录总览

| 目录 | 作用 |
|------|------|
| `User/` | **业务代码**：通信、传感器、导航、LED |
| `Core/` | CubeMX 生成的外设初始化 + `main.c` + cJSON |
| `Drivers/STM32F4xx_HAL_Driver/` | ST HAL 驱动（工程只编译其中一部分） |
| `Drivers/CMSIS/` | CMSIS 内核/器件头文件；另含 DSP、DAP 等未参与编译的官方包 |
| `MDK-ARM/` | Keil 工程、启动文件、链接脚本、编译产物 |
| `.mxproject` | STM32CubeMX 生成记录，不是源码 |

---

## 3. User/ — 业务模块（日常维护重点）

`.c` 与 `.h` 成对：头文件定义接口/宏/数据结构，源文件实现。

### 3.1 `protocol.c` / `protocol.h` — 上位机 JSON 通信

**做什么：** PC ↔ STM32 的 JSON 协议。UART4 空闲中断收一整帧，主循环里用 cJSON 解析；遥测用 `snprintf` 打包后阻塞发送。

**关键约定：**

- 串口：`huart4`，115200，RX 缓冲 512 字节
- 断连超时：`PROTOCOL_DISCONNECT_TIMEOUT_MS = 5200`（从未成功收过包时不算超时）
- ISR 只拷贝数据、置 `s_rx_pending`，**不在中断里 malloc / 解析 JSON**
- 发送遥测时调用 `LED_Toggle()`，可作为通信心跳指示

**上位机下发字段（`PCCommand_t`）：**

| 字段 | 含义 |
|------|------|
| `control_mode` | 字符串：`manual` / `navigate` / `stable_anchor` / `cruise_speed` / `cruise_dir` / `fixed_point` |
| `move_speed` / `steer_speed` | [-100, 100]，前正后负，左正右负 |
| `target_lat` / `target_lon` | 导航目标 WGS84 |
| `return_lat` / `return_lon` | 返航点 |
| `dc_prot` | 1=断连后导航返航点；0=断连后停电机（或稳泊，见 `main.c` 宏） |
| `lb_prot` | 1=低电量保护使能 |

**STM32 上报字段（`STM32Telemetry_t`）：** GPS 有效、是否到达、航向/横滚/俯仰、电量、经纬、速度(m/s)、海拔、gz、当前速度指令、当前目标、距离误差、航向误差。

**改协议时：** 同时改解析、打包、以及 `main.c` 里对字段的使用。cJSON 解析会吃堆，启动文件堆已加大到 8KB。

---

### 3.2 `navigation_controller.c` / `navigation_controller.h` — 导航控制

**做什么：** 根据当前经纬、航向、偏航角速度 `gz`，输出行进速度和转向速度（[-100, 100]）。

**控制结构（增量式 PI，换目标会清零累加器）：**

- 转向外环：航向误差 → 目标转向量
- 转向内环：转向量误差（反馈 `gz`）→ 差速舵令
- 行进环：沿当前船首向的目标点距离误差 → 行进速度
  - 设定点是**目标点本身**，不是 2m 圈缘（旧的 `distance - 2m` 会把船停在圈上，顶风进不了圈）
  - 限幅 `[0, max]`，**禁止倒车**；越过目标或船首没对准时速度收到 0，出圈后转向再前进回来
- 2m 到达圈（`NAV_ARRIVAL_THRESHOLD = 2.0f`）**只做模式切换**，不是位置环设定点：
  - 圈内：清转向积分，锁定目标船首向（优先上一帧目标船首向，无则保持当前船首向），继续稳泊输出
  - 圈外：导航回目标点（目标船首向 = 目标方位角）
- 原「到达停机」「航向误差 > 32° 停速原地转向」已停用（源码里 `#if 0` 保留）

**地理计算：** Haversine 距离、方位角、沿航向推算下一点（定向巡航用）。

**整定宏（头文件）：** `NAV_HEADING_KP/KI`、`NAV_TURN_RATE_KP/KI`、`NAV_POSITION_KP/KI`、`NAV_DEFAULT_MAX_SPEED`（80）。

**注意：** 改到达圈半径只改 `NAV_ARRIVAL_THRESHOLD`（只影响锁船首向的切换距离，不影响位置环设定点）。圈内/圈外切换只清转向积分，不清行进环积分（留下的速度积分用来顶风）。

**目标 (0,0) 会被忽略**，避免误设无效目标。

---

### 3.3 `gnss_sensor.c` / `gnss_sensor.h` — GNSS（Modbus RTU）

**做什么：** USART2、4800、从站 `0x50`，功能码 0x03，读寄存器 `0x0049` 起 8 个寄存器。

- 请求：`50 03 00 49 00 08 98 5B`
- 响应 21 字节：地址 + 功能码 + 长度 + 16 字节数据 + CRC
- 输出：经纬（度分合成十进制度）、海拔(m)、航向(°)、地速

**增强：** 超过 500ms 无成功采集 → `TIMEOUT`；帧头连续错 >3 次才算硬错误，失败时保留上次有效值。

**速度单位：** 模块内部按 **km/h** 解析（`/1000`）；`main.c` 上报前再 `/3.6` 转成 m/s。头文件结构体注释写成 m/s，与实现不一致，改解析时不要漏掉 `main.c` 的换算。

---

### 3.4 `heading_sensor.c` / `heading_sensor.h` — 航向 IMU（Modbus RTU）

**做什么：** USART3、4800、从站 `0x60`，读寄存器 `0x0037` 起 9 个寄存器。

- 请求：`60 03 00 37 00 09 3C 73`
- 响应 23 字节
- 输出：roll / pitch / yaw，以及 gx / gy / **gz**（导航内环用 gz）

**航向处理：** 原始 yaw 取反（逆时针→顺时针）+ 磁偏角 `HEADING_SENSOR_MAG_DECLINATION`（当前 -3.27°）+ 归一到 [0, 360)。换地区先改磁偏角宏。

新鲜度 / 帧头容错逻辑与 GNSS 相同（500ms / 连续 3 次）。

---

### 3.5 `bms_sensor.c` / `bms_sensor.h` — BMS 电量（Modbus RTU）

**做什么：** USART1、9600、从站 `0x01`，读寄存器 `0x0028`（RSOC，低字节为 0~100%）。

**当前状态：** `main.c` 里 `BMSSensor_Init()` 会调用，但 `BMSSensor_Update()` **已被注释**，`s_battery_level` 一直为 0。低电量保护因 `s_battery_level != 0` 条件而实际上不会触发。要启用：取消注释更新调用，并确认 `BATTERY_LOW_VOLTAGE` 比较的是电压还是 RSOC（宏注释与阈值 10.0 目前混用）。

---

### 3.6 `led.c` / `led.h` — 状态灯

**做什么：** PB12，低电平亮。`LED_Blink()` 非阻塞，由 `SysTick_Handler` 每 1ms 调 `LED_Blink_Callback()`。启动时闪 10 次；每次成功发遥测会 `LED_Toggle()`。

闪烁进行中会拒绝新的 `LED_Blink` 请求。

---

## 4. Core/ — 应用入口与外设初始化

### 4.1 `Core/Src/main.c` — 程序入口与系统调度

**做什么：** HAL / 时钟 / 外设初始化，再初始化各 User 模块，然后进入主循环。业务逻辑几乎都在本文件的 `USER CODE` 段。

**控制模式行为：**

| 模式 | 行为 |
|------|------|
| `MANUAL` | 直接用上位机 `move_speed` / `steer_speed` |
| `NAVIGATE` / `FIXED_POINT` | 用 `move_speed` 作导航最大速度，驶向 `target_*` |
| `STABLE_ANCHOR` | 切入时把当前 GNSS 坐标设为目标，原地稳住 |
| `CRUISE_SPEED` | 定速前进，转向为 0 |
| `CRUISE_DIR` | 切入时记录当前航向，沿该方向每 32m 推一个新目标 |

**保护逻辑：**

- GNSS 或航向连续异常 > `SENSOR_ANOMALY_LIMIT`(3) → 强制手动、速度清零
- `dc_prot=1` 且断连 → 导航到返航点
- `dc_prot=0` 且断连 → 默认停电机（`DISCONNECT_ACTION`）；可改为稳泊
- `lb_prot=1` 且电量低于阈值 → 导航返航；`move_speed==20` 可手动退出低电保护

**驱动板帧（UART6，9 字节）：**

`0x55 | 预留3字节 | move | steer | 前灯 | 后灯 | 0xAA`  
灯亮度字段目前恒为 0。注释里曾写 UART5，实际发送走 **UART6**。

**时钟：** `SystemClock_Config()`：HSE+LSI，PLL M=4 N=168 P=2，Flash latency 5。  
**故障：** `Error_Handler()` 关中断后死循环。

---

### 4.2 `Core/Inc/main.h`

包含 `stm32f4xx_hal.h`，声明 `Error_Handler()`。各模块通过它拿到 HAL 类型。应用级宏不在这里，而在各 User 头文件和 `main.c`。

---

### 4.3 `Core/Src/usart.c` / `Core/Inc/usart.h` — 串口配置

定义 6 路 UART 句柄及 `MX_*_Init`、MSP 引脚复用。`fputc` 被掏空（`printf` 当前不会真正从串口打出）。

| 句柄 | 外设 | 波特率 | 引脚 | 业务用途 |
|------|------|--------|------|----------|
| `huart4` | UART4 | 115200 | PA0 TX / PA1 RX | 上位机 JSON |
| `huart5` | UART5 | 115200 | PC12 TX / PD2 RX | **已初始化，业务未使用** |
| `huart1` | USART1 | 9600 | PA9 TX / PA10 RX | BMS |
| `huart2` | USART2 | 4800 | PA2 TX / PA3 RX | GNSS |
| `huart3` | USART3 | 4800 | PB10 TX / PB11 RX | 航向传感器 |
| `huart6` | USART6 | 9600 | PC6 TX / PC7 RX | 驱动板 |

---

### 4.4 `Core/Src/gpio.c` / `Core/Inc/gpio.h`

打开 GPIOH/A/B/C/D 时钟。

- **PB12**：LED 输出（初始拉低=亮）
- **PC8**：推挽输出，初始拉低。**业务代码未使用**，可能是预留或 CubeMX 残留

---

### 4.5 `Core/Src/can.c` / `Core/Inc/can.h`

CAN1：PA11 RX / PA12 TX。APB1=42MHz，预分频 4，BS1=15、BS2=5 → 约 **500kbps**。`main` 会 `MX_CAN1_Init`，**无收发业务代码**。

---

### 4.6 `Core/Src/i2c.c` / `Core/Inc/i2c.h`

I2C3：100kHz，PA8 SCL / PC9 SDA。仅初始化，**无业务读写**。

---

### 4.7 `Core/Src/iwdg.c` / `Core/Inc/iwdg.h`

独立看门狗：LSI≈32kHz，分频 32，重载 4095 → 超时约 **4.1s**。主循环必须持续 `HAL_IWDG_Refresh`。调试时若卡在断点容易复位。

---

### 4.8 `Core/Src/stm32f4xx_it.c` / `Core/Inc/stm32f4xx_it.h` — 中断

- Cortex 异常：NMI / HardFault / MemManage / BusFault / UsageFault 等（故障类死循环）
- `SysTick_Handler`：`HAL_IncTick()` + `LED_Blink_Callback()`
- USART1/2/3、UART4/5、USART6：转给 `HAL_UART_IRQHandler`
- `HAL_UARTEx_RxEventCallback`：仅 UART4 转给 `Protocol_UART_RxEventCallback`

---

### 4.9 `Core/Src/stm32f4xx_hal_msp.c`

全局 MSP：打开 SYSCFG、PWR。各外设 MSP 写在对应的 `usart.c` / `can.c` / `i2c.c`。

---

### 4.10 `Core/Inc/stm32f4xx_hal_conf.h`

HAL 裁剪与晶振参数。本工程启用：CAN、I2C、IWDG、UART、GPIO、EXTI、DMA、RCC、FLASH、PWR、CORTEX。  
`HSE_VALUE = 8MHz`。增删 HAL 模块时同步改本文件和 Keil 工程里的 `.c` 列表。

---

### 4.11 `Core/Src/system_stm32f4xx.c`

CMSIS 系统文件：`SystemInit()`（复位后、进 `main` 前）、`SystemCoreClock` / `SystemCoreClockUpdate()`。与 `Drivers/CMSIS/.../system_stm32f4xx.c` 同类，**工程实际编译的是 `Core/Src` 这份**。

---

### 4.12 `Core/Src/cJSON.c` / `Core/Inc/cJSON.h`

Dave Gamble 的 cJSON。本工程用于 **解析上位机 JSON**（遥测发送不用它，用 `snprintf`）。依赖堆，堆大小见启动文件。

---

## 5. MDK-ARM/ — 工程与启动

| 文件 | 作用 |
|------|------|
| `STM32F405RGT6.uvprojx` | Keil 工程：源文件列表、编译选项、芯片型号 |
| `STM32F405RGT6.uvoptx` | Keil 调试/界面选项 |
| `STM32F405RGT6.uvguix.*` | 各用户本机窗口布局，可不提交 |
| `startup_stm32f405xx.s` | 启动：向量表、`Reset_Handler`、栈 **16KB**（0x4000）、堆 **8KB**（0x2000）。改堆栈改这里 |
| `STM32F405RGT6/STM32F405RGT6.sct` | 分散加载：Flash `0x08000000` 1MB；SRAM `0x20000000` 112KB + `0x2001C000` 16KB |
| `STM32F405RGT6/` 下 `.hex/.map/.o/.d` | 编译产物，不必当源码维护 |
| `RTE/_STM32F405RGT6/RTE_Components.h` | Keil RTE 占位，当前为空 |
| `EventRecorderStub.scvd` | Event Recorder 桩 |
| `.vscode/` | Keil Assistant 等编辑器配置 |

---

## 6. Drivers/ — 官方库（按是否进工程区分）

Cube 包很大，**不要按目录里每一个未用文件去改**。下面只说明本工程真正用到的，以及其余目录的用途。

### 6.1 已编进工程的 HAL（`Drivers/STM32F4xx_HAL_Driver/Src` + 对应 `Inc`）

| 文件 | 职责 |
|------|------|
| `stm32f4xx_hal.c` | HAL 初始化、Tick、延时 |
| `stm32f4xx_hal_cortex.c` | NVIC、SysTick、MPU |
| `stm32f4xx_hal_rcc.c` / `_ex.c` | 时钟、PLL、外设时钟 |
| `stm32f4xx_hal_gpio.c` | GPIO |
| `stm32f4xx_hal_dma.c` / `_ex.c` | DMA（本应用未开 DMA 传输，库仍链接） |
| `stm32f4xx_hal_pwr.c` / `_ex.c` | 电源、调压 |
| `stm32f4xx_hal_flash.c` / `_ex.c` / `_ramfunc.c` | Flash、等待周期 |
| `stm32f4xx_hal_exti.c` | 外部中断线 |
| `stm32f4xx_hal_uart.c` | 全部 UART/USART |
| `stm32f4xx_hal_i2c.c` / `_ex.c` | I2C3 |
| `stm32f4xx_hal_can.c` | CAN1 |
| `stm32f4xx_hal_iwdg.c` | 独立看门狗 |

同目录其余 `stm32f4xx_hal_adc.c`、`_spi.c`、`_tim.c` 等是完整 HAL 包，**本工程未加入 Keil**。`Inc/Legacy/` 为旧 API 兼容头。

### 6.2 CMSIS Device（本芯片实际用到的头）

路径：`Drivers/CMSIS/Device/ST/STM32F4xx/Include/`

| 文件 | 职责 |
|------|------|
| `stm32f4xx.h` | 系列总入口，按宏选具体型号 |
| `stm32f405xx.h` | F405 寄存器、中断号、外设基址 |
| `system_stm32f4xx.h` | `SystemInit` / `SystemCoreClock` 声明 |

同目录其它 `stm32f407xx.h` 等是别的型号，本工程不编译。`Source/Templates/` 下各工具链启动文件是模板，**实际用的是 `MDK-ARM/startup_stm32f405xx.s`**。

### 6.3 CMSIS Include（内核）

路径：`Drivers/CMSIS/Include/`

本芯片用 **`core_cm4.h`**（Cortex-M4 内核寄存器、NVIC、SysTick）以及 `cmsis_compiler.h`、`cmsis_armcc.h`、`cmsis_version.h`、`mpu_armv7.h` 等被其包含的文件。`core_cm0.h`、`core_cm7.h`、`core_cm33.h` 等是其它内核，不用改。

### 6.4 随 Cube 带来、本固件未使用的目录

| 路径 | 内容 |
|------|------|
| `Drivers/CMSIS/DSP/` | CMSIS-DSP 数学库 |
| `Drivers/CMSIS/DAP/` | CMSIS-DAP 调试固件 |
| `Drivers/CMSIS/RTOS/` | CMSIS-RTOS 模板（工程无 RTOS） |
| `Drivers/CMSIS/Documentation/` | CMSIS 文档 |
| `Drivers/CMSIS/NN/`、`Core/` 等（若存在） | 官方示例/其它组件 |

这些不进当前 `.uvprojx`，对运行无影响。

---

## 7. 根目录其它文件

| 文件 | 作用 |
|------|------|
| `.mxproject` | CubeMX 上次生成的源/头文件列表，重新 Generate 时会对照 |
| `模块说明.md` | 本维护文档 |

工程内没有 `.ioc`；改引脚/外设目前以 `Core/` 源码和 CubeMX 记录为准。

---

## 8. 维护速查

### 8.1 想改什么，去哪

| 需求 | 位置 |
|------|------|
| 控制模式 / 保护策略 / 主循环节拍 | `Core/Src/main.c` |
| JSON 字段、超时、通信串口 | `User/protocol.h` + `.c` |
| PI 参数、到达半径、最大速度 | `User/navigation_controller.h` |
| GNSS 寄存器/波特率/地址 | `User/gnss_sensor.h` |
| 磁偏角、航向量程 | `User/heading_sensor.h` |
| 启用 BMS | `main.c` 取消注释 `BMSSensor_Update`，核对阈值含义 |
| LED 引脚 | `User/led.h` 与 `gpio.c` |
| 波特率 / 引脚 | `Core/Src/usart.c`（CubeMX 重新生成会覆盖 USER CODE 外的部分） |
| 看门狗时间 | `Core/Src/iwdg.c` |
| 堆/栈不够（cJSON 崩） | `MDK-ARM/startup_stm32f405xx.s` |
| 启用/关闭 HAL 模块 | `stm32f4xx_hal_conf.h` + `.uvprojx` |

### 8.2 当前已知缺口（改代码前先看）

1. **BMS 未轮询**，低电量保护实际上不起作用。  
2. **UART5、CAN1、I2C3、PC8** 已初始化，无应用层使用。  
3. **`printf` / `fputc` 为空**，各模块的 `*_DBG` / `*_LOG_PRINT` 默认不会从串口出来。  
4. **到达圈现为 2m 稳泊**（不再进圈停机）；32° 原地转向已停用。  
5. 驱动板发送注释写 UART5，代码走 **UART6**。  
6. GNSS 速度：模块 km/h，遥测 m/s，中间在 `main.c` 换算。

### 8.3 编译注意

- 预定义：`USE_HAL_DRIVER`、`STM32F405xx`
- 包含路径：`Core/Inc`、`User`、`Drivers/STM32F4xx_HAL_Driver/Inc`、`Drivers/STM32F4xx_HAL_Driver/Inc/Legacy`、`Drivers/CMSIS/Device/ST/STM32F4xx/Include`、`Drivers/CMSIS/Include`
- User 源文件均在 `STM32F405RGT6.uvprojx` 的 Application/User 组；新增 `.c` 必须加入工程。

---

*文档根据当前源码整理，后续改模块时请同步更新对应小节。*
